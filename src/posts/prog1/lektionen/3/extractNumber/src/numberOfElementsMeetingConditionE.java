public class numberOfElementsMeetingConditionE {

    /* public static int numberOfElementsMeetingConditionE (SomeType[] arr) {
        // j muss am anfang bereits ausserhalb sein
        int i = 0; int j = arr.length;

        while (i < j) {
            if (E(arr[m])) i = m + 1; else j = m;
        }

        return i;
    } */
}
